apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cockpitApp.fullname" . }}-test-connection"
  labels:
    {{- include "cockpitApp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cockpitApp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

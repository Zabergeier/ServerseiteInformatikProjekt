apiVersion: v1
kind: Service
metadata:
  name: {{ include "cockpitApp.fullname" . }}
  labels:
    {{- include "cockpitApp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cockpitApp.selectorLabels" . | nindent 4 }}
